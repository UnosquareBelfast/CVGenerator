import styled from 'styled-components';

const StyledSelect = styled.div`
  max-width: 500px;
  min-width: 280px;
  padding: 2em;

  label {
    padding-bottom: 5px;
  }
`;

export default StyledSelect;
