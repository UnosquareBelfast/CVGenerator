import styled from 'styled-components';
import { Button } from 'Common';

const StyledGenerateCVButton = styled(Button)`
  height: 50px;
  margin-top: 15%;
  width: 75%;
`;

export default StyledGenerateCVButton;
